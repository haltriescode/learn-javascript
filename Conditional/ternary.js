// Using ternary operator

const price = 100;
let isFriend = true;
const discount = isFriend ? 0.01 : 0; // when true, discount = 0.01; when false, discount = 0

console.log(`Congrats fren, you save $${ price * discount}!`);
